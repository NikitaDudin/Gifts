import { SET_APP_VERSION_AND_BUILD } from '@/store/actionsList';

// eslint-disable-next-line import/prefer-default-export
export const setVersionAndBuild = (payload) => ({ type: SET_APP_VERSION_AND_BUILD, payload });
